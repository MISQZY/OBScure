interface ImportMetaEnv {
  /** Built-in Twitch app Client ID, baked in at build time from .env (see .env.example). Users can override it per-profile via Settings. */
  readonly VITE_TWITCH_CLIENT_ID?: string
}
